---
environments:
  master:
    ssh:
      user: root
      hostname: dev-master.simpledrupalcloud.com
    drupal:
      path: /var/www/project_1
dev:
  up: |
    echo "up"
  down: |
    echo "down"
  destroy: |
    echo "destroy"