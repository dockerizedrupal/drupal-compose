---
environments:
  master:
    ssh:
      user: root
      hostname: dev-master.simpledrupalcloud.com
      private_key:
        path: ~/.ssh/id_rsa
    drupal:
      path: /var/www/drupal
dev:
  up: |
    echo "up"
  down: |
    echo "down"
  destroy: |
    echo "destroy"